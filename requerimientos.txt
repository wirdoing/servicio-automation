instalar pip : sudo apt-get install python3-pip
instalar selenium: sudo pip3 install selenium
instalar pynput: pip3 install pynput
hacer que geckodriver este en el path permanentemente:
cd /etc/
sudo gedit environment
si el documento esta vacio agregar: PATH="/en/donde/se/encuentra/el/webdriver"
si el documento ya tiene un path entonces dentro de las comillas al final poner un ":"
 y poner la ruta donde se encuentra el webdriver
 instalar tkinter: sudo dnf install python3-tkinter